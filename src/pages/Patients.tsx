import type { FC } from 'react'
import PageHeader from '../components/PageHeader'

const Patients: FC = () => (
  <div style={{ padding: 24, fontFamily: 'Manrope, sans-serif', flex: 1 }}>
    <PageHeader title="Patients" />
  </div>
)

export default Patients
